//! Hook context types for tracking message origin and routing responses.
//!
//! These types are defined in `gateway-events` and re-exported here for convenience.

pub use gateway_events::context::{HookContext, HookType};
