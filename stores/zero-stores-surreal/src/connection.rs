//! SurrealDB connection construction.
//!
//! Real implementation lands in Task 3.

use crate::error::SurrealStoreError;

/// Connect to a SurrealDB instance. Placeholder — populated in Task 3.
pub async fn connect(_url: &str) -> Result<(), SurrealStoreError> {
    Err(SurrealStoreError::NotImplemented)
}
