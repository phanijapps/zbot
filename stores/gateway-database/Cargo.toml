# ============================================================================
# GATEWAY DATABASE (DEPRECATED SHIM — Slice D8)
#
# This crate's contents were merged into `zero-stores-sqlite` in Slice D8.
# It re-exports the public surface so the import migration can land as a
# separate green-build commit. The next commit deletes this crate.
# ============================================================================

[package]
name = "gateway-database"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Deprecated shim — see zero-stores-sqlite"

[dependencies]
zero-stores-sqlite = { path = "../zero-stores-sqlite" }

[lints]
workspace = true
